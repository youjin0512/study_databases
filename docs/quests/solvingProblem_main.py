import solvingProblem_functions # SolvingProblem_functions라는 파이썬 파일(모듈)을 호출한다.

collection = solvingProblem_functions.connect() #호출한 파읠의 Connect라는 function을 호출하여 collection이라는 변수로 지정한다.

solvingProblem_functions.insert_quiz_list()

solvingProblem_functions.run() 
# 위 과정을 실행하는 function을 호출하여 동작시킨다.